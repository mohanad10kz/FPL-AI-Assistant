# 06 - transfer_planner.py

## الهدف من هذا الملف
إدارة قائمة التحويلات المقترحة **عبر عدة جولات متتالية**، بحيث لا يُقترح أبداً عدد تحويلات أكبر من الرصيد المجاني المتاح (تفادي أي خصم -4/-8 غير مبرَّر بوضوح)، مع إعادة تقييم كاملة كل جولة بدل تنفيذ قائمة قديمة بشكل أعمى.

## الارتباط بباقي الملفات
- يستقبل مرشحي التحويل الخام من `decision_engine.py` (بعد أن يجمع الأخير بيانات `injuries_source.py` + `top_managers.py`).
- يقرأ/يكتب ملف حالة دائم: `data/state/transfer_queue.json` — **هذا الملف الوحيد بالمشروع الذي يملك حالة تُقرأ وتُكتب بين تشغيلات مختلفة** (كل باقي الملفات عديمة الحالة/stateless).
- يُرجع القرار النهائي (من ينفَّذ الآن، من يبقى بالانتظار) لـ `decision_engine.py` ليكمل بناء التوصية النهائية.

## لماذا هذا الملف ضروري (وليس مجرد تفصيل بـ decision_engine)؟
لأن القرار هنا **يمتد عبر الزمن** وليس لحظياً: لو رصيدك تحويلين فقط لكن يوجد 4 مرشحين، لا يكفي "اختر أفضل 2 وانسَ الباقي" — يجب حفظ الاثنين المتبقيين ومراجعتهم بالجولة القادمة **من جديد بالكامل**، لأن وضعهم قد يتغيّر (تعافي من إصابة، ظهور بديل أفضل، تراجع أهمية التحويل).

## هيكل ملف الحالة `data/state/transfer_queue.json`
```json
{
  "last_updated_gw": 6,
  "pending_candidates": [
    {
      "player_out_id": 234,
      "player_out_name": "اسم اللاعب",
      "reason": "غير موجود عند أي من أفضل 10 + حالة: Doubtful",
      "priority_score": 8.2,
      "flagged_at_gw": 5,
      "reviewed_count": 1
    }
  ]
}
```

## خطوات التنفيذ التفصيلية

### 1. `load_queue()` / `save_queue(queue_data)`
قراءة/كتابة JSON بسيطة. **معالجة إلزامية:** لو الملف غير موجود (أول تشغيل)، أنشئ هيكل فارغ بدل رمي خطأ.

### 2. `calculate_priority_score(candidate)`
معادلة ترتيب الأولوية (كل عنصر بوزن واضح بأعلى الملف كثوابت قابلة للتعديل):
- إصابة مؤكدة (`status == "injured"` أو `"suspended"`) → أولوية قصوى ثابتة (مثال: درجة 10).
- غياب عن إجماع أفضل N + حالة `"doubtful"` → درجة متوسطة-عالية (مثال: 6-8، تتدرّج حسب نسبة الغياب).
- فرق بسيط بالنقاط المتوقعة بدون أي إشارة إصابة/غياب → درجة منخفضة (مثال: 1-3).

### 3. `update_transfer_queue(new_candidates, available_free_transfers)`
**الدالة الأساسية — منطقها بالتفصيل:**

```
1. اقرأ pending_candidates من الملف المحفوظ (الجولة الماضية).
2. لكل مرشح قديم:
   أ. أعد حساب حالته بالبيانات الجديدة لهذه الجولة (هل لسا مصاب؟ هل تحسّن؟).
   ب. لو لم يعد هناك سبب وجيه للتحويل (تعافى، أصبح أساسي مؤكد) → احذفه من القائمة نهائياً.
   ج. لو السبب لسا قائماً → أعد حساب priority_score من جديد ببيانات محدّثة (لا تستخدم الدرجة القديمة كما هي).
3. ادمج المرشحين القدامى المُحدَّثين مع new_candidates (مرشحين هذه الجولة) بقائمة واحدة.
4. احذف أي تكرار (لو نفس اللاعب ظهر بالقديم والجديد، خذ نسخة واحدة بأحدث بيانات).
5. رتّب القائمة الموحّدة تنازلياً حسب priority_score.
6. execute_now = أعلى (available_free_transfers) عنصر من القائمة المرتبة.
7. still_pending = الباقي.
8. احفظ still_pending بملف الحالة للجولة القادمة (مع تحديث last_updated_gw).
9. أرجع {"execute_now": [...], "still_pending": [...]}
```

### 4. حالة خاصة: تحويل إضافي بخصم -4 يستحق العناء
إذا أعلى مرشح **بعد** استهلاك كل التحويلات المجانية له `priority_score` أعلى من عتبة معينة (مثال: إصابة مؤكدة لأساسي مهم) — يُسمح باقتراح تحويل واحد إضافي بخصم -4 **فقط**، ويُذكر بوضوح بالتقرير سبب تبرير الخصم. لا يُسمح أبداً باقتراح تحويلين إضافيين أو أكثر بنفس الجولة تلقائياً — أي حالة تتطلب أكثر من ذلك تُعرض للمستخدم كخيار (مثال: "فكّر باستخدام Wildcard بدل تحمّل خصم مضاعف").

## معايير القبول
- [ ] القائمة المحفوظة تُقرأ وتُكتب بشكل صحيح بين تشغيلات متتالية (اختبر بتشغيلين متتاليين ببيانات مختلفة).
- [ ] لاعب تعافى من إصابته يُحذف تلقائياً من القائمة بالمراجعة التالية.
- [ ] عدد `execute_now` لا يتجاوز أبداً `available_free_transfers` إلا بالحالة الاستثنائية الموثّقة أعلاه (وبحد أقصى تحويل واحد إضافي).
- [ ] لا يوجد تكرار لنفس اللاعب بالقائمة النهائية.
