# 08 - report_builder.py

## الهدف من هذا الملف
تحويل الكائن الناتج من `decision_engine.py` (بيانات JSON بنيوية) إلى **نص مقروء بالعربية** بصيغة Markdown جاهز للإرسال عبر تيليجرام.

## الارتباط بباقي الملفات
- المدخل الوحيد: الكائن النهائي من `decision_engine.py` (راجع بنيته بملف `07_decision_engine.md`).
- المخرج: نص جاهز يُمرَّر مباشرة لـ `telegram_notifier.send_report()`.
- نسخة من نفس النص (أو الكائن الأصلي) تُحفظ أيضاً بـ `data/history/gw_{n}.json` من طرف `main.py` للأرشفة.

## خطوات التنفيذ

### 1. `build_report_text(decision_data)`
دالة واحدة تبني النص كاملاً بالترتيب التالي بالضبط (الترتيب مهم — من الأكثر إلحاحاً للأقل):

```
📊 تقرير الجولة #{gameweek}

✅ التحويلات المنفَّذة الآن ({عدد forced_transfers الذي دخل execute_now}):
[لكل تحويل] - إخراج: {out} ← إدخال: {in}
   السبب: {reason}

⏳ تحويلات مؤجَّلة (بانتظار رصيد تحويلات كافٍ):
[لكل عنصر still_pending] - {out}: {reason}
   (لا داعي لخصم نقاط الآن — سيُعاد تقييمه الجولة القادمة)

⚠️ لاعبون يحتاجون مراجعتك (إشارة غير مؤكدة، القرار لك):
[لكل flagged_player] - {name}: {reason}

©️ الكابتن المقترح: {captain}
🥈 نائب الكابتن: {vice_captain}

🎯 اقتراح رقاقة: {نعم/لا حسب chip_suggestion.recommended}
   [إن وُجد] النوع: {chip} | السبب: {reason}

💰 الرصيد المتوقع بعد التحويلات: £{bank_after}م
🔄 التحويلات المجانية المتبقية: {free_transfers_remaining}
❗ خصم نقاط متوقع: {transfers_cost} نقطة
```

### 2. قواعد صياغة إلزامية
- **لا تصغ أي توصية كأمر نهائي.** استخدم دائماً صيغة "مقترح"، "للمراجعة"، وليس "افعل كذا".
- لو قائمة معينة فارغة (مثال: لا يوجد `still_pending`)، احذف قسمها بالكامل من النص بدل عرضه فارغاً — التقرير يجب أن يبقى مختصراً ومركّزاً.
- التزم بحدود Markdown المدعومة من Telegram Bot API (لا تستخدم جداول HTML كاملة، فقط `*bold*`, `_italic_`, أسطر جديدة عادية).

## معايير القبول
- [ ] النص الناتج يُطابق الترتيب المحدد أعلاه بالضبط.
- [ ] لا يظهر أي قسم فارغ بالتقرير النهائي.
- [ ] كل التوصيات مصاغة بصيغة اقتراح لا أمر.
- [ ] النص يمر بنجاح عبر Telegram Bot API بدون أخطاء تنسيق (اختبر بإرسال فعلي).
