# 03 - squad_optimizer.py

## الهدف من هذا الملف
بناء **أفضل تشكيلة ممكنة قبل بداية الموسم (الجولة 1)**، وقت لا تتوفر بيانات "أفضل 10 مدراء عالمياً" لأنه لا توجد نتائج فعلية بعد. **يُستخدم مرة واحدة فقط** بحياة المشروع (أو عند إعادة بناء كاملة لاحقاً باستخدام Wildcard).

## الارتباط بباقي الملفات
- يستدعي `fpl_client.get_bootstrap_static()` لجلب بيانات كل اللاعبين.
- مخرجاته (قائمة الـ15 لاعب) تُستخدم يدوياً من المستخدم لبناء تشكيلته الفعلية داخل موقع/تطبيق FPL (تذكير: **لا يوجد تنفيذ تلقائي داخل الحساب** — هذا الملف يعطي توصية فقط عبر التقرير).
- بعد الجولة 1، هذا الملف **لا يُستدعى مرة أخرى** — `top_managers.py` و`decision_engine.py` يأخذون المهمة بشكل دائم.

## لماذا لا نستخدم "أفضل 10 عالمياً" هنا؟
لأنه قبل انطلاق أي جولة، لا توجد نتائج ولا تشكيلات فعلية لأي مدير — كل الدوري لسا ما بدأ. المقارنة بأفضل 10 مستحيلة تقنياً بهذه المرحلة، فنحتاج بديل استباقي.

## البديل: نسب الامتلاك قبل الموسم (Pre-season Ownership)
`selected_by_percent` بالـ API يعكس اختيارات مئات الآلاف من اللاعبين **قبل** انطلاق الموسم — وهذا يشمل عملياً كثير من اللاعبين المحترفين اللي سيصبحون لاحقاً من أفضل المدراء. لذلك هو مؤشر معقول (مو مثالي، لكن أفضل بديل متاح) لتوجيه القرار الأولي.

## خطوات بناء درجة كل لاعب (Scoring Formula)

لكل لاعب، احسب درجة مركّبة من 4 عناصر (كل عنصر يُطبَّع Normalize بين 0-1 قبل الجمع، لتفادي هيمنة عنصر واحد بسبب اختلاف المقاييس):

1. **نسبة الامتلاك (`selected_by_percent`)** — وزن مقترح: 35%
2. **أداء الموسم الماضي (`total_points` من بيانات الموسم السابق إن توفرت بـ bootstrap-static، أو `points_per_game`)** — وزن مقترح: 30%
3. **سهولة أول 5 مباريات (Fixture Difficulty Rating، من endpoint `/fixtures/`، معكوسة: كل ما كانت أسهل كل ما زادت الدرجة)** — وزن مقترح: 20%
4. **القيمة مقابل السعر (`points_per_game / now_cost`)** — وزن مقترح: 15%

**ملاحظة للـ AI agent:** هذه الأوزان قابلة للتعديل بسهولة (اجعلها ثوابت بأعلى الملف، لا تُدفنها بمنطق الحساب) — ضع تعليقاً واضحاً يشرح أن المستخدم يقدر يجرّب أوزان مختلفة لاحقاً.

## مسألة التحسين (Optimization Problem)

استخدم مكتبة `PuLP` (Linear Programming، مجانية مفتوحة المصدر) لحل:

**الهدف:** تعظيم مجموع الدرجات المركّبة لـ15 لاعب مُختارين.

**القيود الإلزامية:**
- مجموع الأسعار ≤ 100.0 مليون.
- بالضبط: 2 GK + 5 DEF + 5 MID + 3 FWD.
- بالضبط 3 لاعبين كحد أقصى من نفس `team` (النادي).
- كل لاعب يُختار مرة واحدة فقط (متغير ثنائي 0/1 لكل لاعب — نموذج Binary Integer Programming كلاسيكي).

## اختيار التشكيلة الأساسية من الـ15
بعد اختيار الـ15، اختر أفضل 11 (حسب نفس الدرجة) ضمن تشكيلة صحيحة (1 GK بالضبط، ≥3 DEF، ≥1 FWD) — جرّب أكثر من تشكيلة مسموحة (3-4-3, 4-4-2, إلخ) واختر الأعلى درجة إجمالية.

## اختيار الكابتن ونائبه
أعلى لاعبَين درجة بالتشكيلة الأساسية (ليس بالضرورة الأعلى سعراً).

## معايير القبول
- [ ] الناتج يحقق كل القيود (ميزانية، توزيع مراكز، حد الأندية) بدون استثناء.
- [ ] تشغيل الدالة مرتين بنفس البيانات يُعطي نفس النتيجة (حتمية، لا عشوائية).
- [ ] الأوزان قابلة للتعديل من ثوابت واضحة بأعلى الملف.
- [ ] الناتج يتضمن: التشكيلة الكاملة (15) + الأساسية (11) + البدلاء (4) + الكابتن + نائب الكابتن.
