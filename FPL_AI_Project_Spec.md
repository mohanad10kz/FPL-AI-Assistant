# مواصفات مشروع: FPL AI Assistant

## نظرة عامة على المشروع

نظام أوتوماتيكي أسبوعي (بدون تدخل بشري إلا القرار النهائي) يقوم بـ:
1. سحب بيانات فريق المستخدم من FPL API الرسمي.
2. سحب بيانات تشكيلات أفضل 10 مدراء عالمياً (بعد انتهاء أول جولة).
3. سحب بيانات إصابات من مصدر جاهز ومصنّف.
4. تحليل ومقارنة تشكيلة المستخدم بالأفضل عالمياً.
5. اقتراح تحويلات + كابتن + توقيت Chips.
6. إرسال تقرير مفصّل عبر Telegram Bot.
7. المستخدم يتخذ القرار النهائي يدوياً داخل حساب FPL.

**لا يوجد تنفيذ تلقائي داخل حساب FPL. لا يوجد اشتراك مدفوع بأي API. لا يوجد AI API خارجي في النسخة الأولى.**

---

## المكدس التقني (Tech Stack)

| المكوّن | الأداة | السبب |
|---|---|---|
| اللغة | Python 3.11+ | دعم واسع لمكتبات FPL وscraping |
| سحب بيانات FPL | `requests` + FPL API الرسمي | مجاني، بدون مفتاح API |
| سحب إصابات | `requests` + `BeautifulSoup4` | scraping خفيف لمصدر مصنّف |
| التخزين | ملفات JSON محلية داخل الـ repo (`/data`) | لا حاجة لقاعدة بيانات بهذا الحجم |
| الإشعارات | Telegram Bot API (`python-telegram-bot` أو `requests` مباشرة) | مجاني بالكامل |
| الجدولة والتشغيل | GitHub Actions (cron) | مجاني، لا يعتمد على تشغيل جهازك |
| إدارة الأسرار | GitHub Secrets | لحماية Telegram Token و FPL Team ID |

---

## هيكل المجلدات

```
fpl-ai-assistant/
├── .github/
│   └── workflows/
│       └── weekly_run.yml          # جدولة GitHub Actions
├── src/
│   ├── __init__.py
│   ├── config.py                   # قراءة المتغيرات البيئية/الأسرار
│   ├── fpl_client.py                # كل اتصالات FPL API
│   ├── injuries_source.py           # سحب بيانات الإصابات
│   ├── top_managers.py              # سحب وتحليل أفضل 10 عالمياً
│   ├── decision_engine.py           # منطق التحويلات/الكابتن/الـChips
│   ├── report_builder.py            # بناء نص التقرير
│   ├── telegram_notifier.py         # إرسال الرسالة لتيليجرام
│   └── main.py                      # نقطة التشغيل الرئيسية (orchestration)
├── data/
│   ├── cache/                       # تخزين مؤقت لتفادي طلبات متكررة
│   └── history/                     # أرشيف تقارير كل جولة (لمراجعة لاحقة)
├── tests/
│   └── test_decision_engine.py
├── .env.example                     # نموذج للمتغيرات المطلوبة (بدون قيم حقيقية)
├── requirements.txt
├── README.md
└── Makefile                         # اختصارات تشغيل (run, test, lint)
```

---

## تفصيل كل وحدة (Module)

### 1. `config.py`
**المسؤولية:** قراءة الإعدادات من متغيرات البيئة (لا يوجد أي قيمة حساسة مكتوبة بالكود).

**المتغيرات المطلوبة:**
```
FPL_TEAM_ID=xxxxxx
TELEGRAM_BOT_TOKEN=xxxxxx
TELEGRAM_CHAT_ID=xxxxxx
INJURY_SOURCE_URL=https://... (قابل للتغيير لاحقاً)
TOP_N_MANAGERS=10
```

---

### 2. `fpl_client.py`
**المسؤولية:** كل التفاعل مع FPL API الرسمي (بدون تسجيل دخول، endpoints عامة).

**الدوال المطلوبة:**
- `get_bootstrap_static()` → يجلب: كل اللاعبين، أسعارهم، فورمهم، نسب امتلاكهم، الفرق، الجولة الحالية.
- `get_current_gameweek()` → يحدد رقم الجولة الحالية/القادمة من `bootstrap-static`.
- `get_my_team(team_id)` → يجلب تشكيلة المستخدم الحالية + رصيد الميزانية + التحويلات المتاحة + الـChips المستخدمة سابقاً.
  - Endpoint: `/entry/{team_id}/event/{gw}/picks/`
- `get_manager_history(team_id)` → لمعرفة الـChips المستخدمة سابقاً (لتفادي اقتراح Chip مستخدم).
- `get_league_standings(league_id)` (اختياري لاحقاً لو أردت مقارنة بمني ليج خاص).

**ملاحظة مهمة للـ AI agent:** كل الـ endpoints هذه عامة (public) ولا تحتاج تسجيل دخول، فقط `team_id` رقمي. لا تُستخدم أي كوكيز أو بيانات دخول شخصية.

---

### 3. `top_managers.py`
**المسؤولية:** جلب تشكيلات أفضل 10 مدراء عالمياً ومقارنتها بتشكيلة المستخدم.

**آلية العمل:**
1. جلب قائمة أفضل المدراء عالمياً عبر: `/leagues-classic/314/standings/` (314 = الدوري العام العالمي "Overall").
2. لكل مدير من أفضل 10، استخراج `entry id` تبعه.
3. استدعاء `get_my_team(entry_id)` (نفس دالة `fpl_client`) لجلب تشكيلة كل واحد منهم.
4. حساب "نسبة تكرار كل لاعب" بين العشرة (مثال: صلاح موجود عند 9 من 10).

**الدالة الأساسية:**
```
compare_my_team_with_top_n(my_squad: list, top_n_squads: list[list]) -> dict
```
**تُرجع:**
```json
{
  "players_not_in_top10": ["اسم اللاعب - لم يظهر عند أي من أفضل 10"],
  "players_low_presence": ["اسم اللاعب - موجود عند 1-2 فقط من أفضل 10"],
  "players_high_consensus_missing": ["لاعب موجود عند 7+ من أفضل 10 وأنت لا تملكه"]
}
```

**⚠️ ملاحظة منهجية مهمة يجب توضيحها بالتقرير النهائي:**
غياب لاعب من تشكيلات أفضل 10 لا يعني بالضرورة أنه لن يلعب أساسي — قد يكون فقط غير مفضّل استراتيجياً (سعره مرتفع، أو المدراء الكبار مركزين على لاعبين آخرين). لذلك يُعرض هذا كـ **"إشارة تحتاج مراجعتك"** وليس قراراً نهائياً، ويُقرن دائماً بحالة الإصابة الفعلية من `injuries_source.py` قبل الحكم عليه.

---

### 4. `injuries_source.py`
**المسؤولية:** جلب حالة كل لاعب (Fit / Doubtful / Injured / Suspended) من مصدر مصنّف جاهز.

**آلية العمل:**
- Scraping بسيط بـ `BeautifulSoup4` لصفحة إصابات مصنّفة (مثال: Premier Injuries، أو مصدر مشابه يوفر جدول حالة/نسبة لياقة).
- تحويل النتائج لقاموس: `{player_name: {"status": "Doubtful", "chance_of_playing": 75}}`

**الدالة الأساسية:**
```
get_injury_status(player_name: str) -> dict
```

**احتياط تقني:** FPL API نفسه يوفر أيضاً حقل `chance_of_playing_next_round` داخل `bootstrap-static` — استخدمه كمصدر أساسي احتياطي في حال فشل الـ scraping (أكثر استقراراً لأنه رسمي).

---

### 4.5 `squad_optimizer.py` (يُستخدم مرة واحدة فقط — بناء تشكيلة الجولة الأولى)

**المسؤولية:** بناء أفضل تشكيلة ممكنة قبل بداية الموسم، حين لا تتوفر بيانات "أفضل 10 عالمياً" بعد (لا يوجد تشكيلات فعلية قبل انطلاق الدوري).

**البديل المستخدم بدل مقارنة أفضل 10:** نسب الامتلاك قبل بداية الموسم (Pre-season ownership%) — تمثل "حكمة الجمهور" ومؤشر مبكر جيد.

**مدخلات الحساب لكل لاعب:**
- نسبة الامتلاك قبل الموسم (وزن أساسي).
- أداء/نقاط الموسم الماضي (متاحة عبر `bootstrap-static` حتى قبل انطلاق الموسم الجديد).
- سهولة أول 4-5 مباريات (Fixture Difficulty Rating من FPL API).
- القيمة مقابل السعر (نقاط متوقعة / السعر).

**الأداة:** مكتبة `PuLP` (Linear Programming، مجانية ومفتوحة المصدر) لحل مشكلة تحسين مقيّدة:
- الهدف: أعلى مجموع "درجة" ممكنة.
- القيود: ميزانية 100 مليون، 15 لاعب، توزيع مراكز صحيح (2GK/5DEF/5MID/3FWD)، حد أقصى 3 لاعبين من نفس النادي.

**الدالة الأساسية:**
```
build_initial_squad(players_data: list, budget: float = 100.0) -> dict
```
**تُستخدم مرة واحدة قبل الجولة 1 فقط.** بداية من الجولة 2، `decision_engine.py` + `top_managers.py` يأخذون المهمة.

---

### 4.6 `transfer_planner.py` (طابور التبديلات متعدد الجولات)

**المسؤولية:** إدارة قائمة التحويلات المقترحة عبر عدة جولات بدل تنفيذها دفعة وحدة وبالسالب.

**المشكلة اللي يحلّها:** لو النظام اكتشف أن 4 لاعبين يحتاجون تبديل بجولة معينة، لكن المستخدم يملك تحويلين مجانيين فقط — لا يُقترح تحويل الأربعة دفعة وحدة (بخصم -4 أو -8)، بل:
1. يُرتَّب الأربعة حسب الأولوية (إصابة مؤكدة > غياب عن إجماع أفضل 10 مع Doubtful > فروقات نقاط متوقعة بسيطة).
2. يُنفَّذ أفضل 2 هذه الجولة فقط.
3. الاثنين المتبقيين يُحفظون كـ"مرشحين معلّقين" بملف حالة دائم.

**ملف الحالة الدائم:** `data/state/transfer_queue.json`
```json
{
  "pending_candidates": [
    {"player_out": "...", "reason": "...", "priority_score": 8.2, "flagged_at_gw": 5}
  ]
}
```

**آلية إعادة التقييم بالجولة التالية (مهم جداً):**
النظام **لا يعيد تنفيذ نفس القائمة تلقائياً**. بدل ذلك:
1. يعيد حساب حالة كل "مرشح معلّق" من الأساس ببيانات الجولة الجديدة (هل لسا سيء؟ تحسّن؟ تعافى من الإصابة؟).
2. يقارنه بأي مرشحين جدد ظهروا حديثاً بنفس الجولة.
3. يدمج القائمتين ويعيد ترتيب الأولوية من جديد، ثم يطبّق نفس القيد (عدد التحويلات المجانية المتاحة فقط).

**الدالة الأساسية:**
```
update_transfer_queue(new_candidates: list, existing_queue: list, available_free_transfers: int) -> dict
```
**تُرجع:** `{"execute_now": [...], "still_pending": [...]}`

---

### 5. `decision_engine.py`
**المسؤولية:** المنطق المركزي لاتخاذ القرار — هذا قلب المشروع.

**المدخلات:** تشكيلتي الحالية + بيانات اللاعبين + بيانات أفضل 10 + بيانات الإصابات + عدد التحويلات المتاحة + Chips المتبقية.

**المخرجات (Recommendation object):**
```json
{
  "suggested_transfers": [
    {"out": "لاعب أ", "in": "لاعب ب", "reason": "...", "cost_in_points": -4 أو 0}
  ],
  "captain": "اسم اللاعب",
  "vice_captain": "اسم اللاعب",
  "chip_suggestion": {"chip": "Triple Captain", "recommended": true/false, "reason": "..."},
  "flagged_players": [
    {"name": "...", "reason": "غير موجود عند أي من أفضل 10 + حالة إصابة: Doubtful"}
  ]
}
```

**قواعد المنطق (Rules-based، بدون AI API):**
1. **فلترة أولية بالإصابات:** أي لاعب حالته Injured/Suspended → اقتراح تحويل مباشر (أولوية قصوى).
2. **فلترة ثانوية بالإجماع:** لاعب غير موجود عند أي من أفضل 10 + حالة إصابته Doubtful أو غير مؤكدة → يُدرج في `flagged_players` (**لا يُقترح تحويله تلقائياً**، فقط يُذكر للمستخدم ليقرر).
3. **حساب تكلفة التحويلات:** لا تتجاوز عدد التحويلات المجانية المتاحة إلا لو الفارق بالنقاط المتوقعة يبرر خصم الـ -4.
4. **الكابتن:** يُختار بناءً على (الفورم + سهولة المباراة القادمة + نسبة التكرار عند أفضل 10).
5. **الـChips:** تُقترح فقط بناءً على شروط واضحة (مثال: Triple Captain يُقترح فقط إذا كان لاعبك المرشح للكباتنة عنده مباراة سهلة جداً + فورم عالي جداً + الجولة القادمة "Double Gameweek" إن وجدت). لا يُقترح Chip مستخدم سابقاً (يُتحقق من `get_manager_history`).

---

### 6. `report_builder.py`
**المسؤولية:** تحويل مخرجات `decision_engine` لنص تقرير مقروء بالعربية لتيليجرام (Markdown formatting).

**شكل التقرير المقترح:**
```
📊 تقرير الجولة #[رقم الجولة]

✅ التحويلات المقترحة:
- إخراج: [لاعب] ← إدخال: [لاعب] | السبب: [...]

⚠️ لاعبين تحتاج مراجعتك (غير مؤكدين بس مو حكم نهائي):
- [لاعب]: [السبب]

©️ الكابتن المقترح: [لاعب] | نائب: [لاعب]

🎯 اقتراح Chip: [نعم/لا] - [السبب]

💰 رصيدك بعد التحويلات: [X]
🔄 تحويلاتك المتبقية: [X]
```

---

### 7. `telegram_notifier.py`
**المسؤولية:** إرسال نص التقرير عبر Telegram Bot API.

**الدالة الأساسية:**
```
send_report(bot_token: str, chat_id: str, message: str) -> bool
```
استخدام endpoint: `https://api.telegram.org/bot{token}/sendMessage` مع `parse_mode=Markdown`.

**إعداد البوت (خطوات يدوية لمرة واحدة، يشرحها الـ AI agent بالـ README):**
1. فتح محادثة مع `@BotFather` في تيليجرام.
2. إرسال `/newbot` واتباع التعليمات → الحصول على `TELEGRAM_BOT_TOKEN`.
3. إرسال أي رسالة للبوت الجديد، ثم زيارة `https://api.telegram.org/bot<TOKEN>/getUpdates` لاستخراج `chat_id`.

---

### 8. `main.py`
**المسؤولية:** تنسيق تسلسل التشغيل الكامل (orchestration).

**التسلسل:**
```
1. تحميل الإعدادات (config.py)
2. جلب bootstrap-static + تشكيلتي (fpl_client.py)
3. إذا الجولة > 1: جلب تشكيلات أفضل 10 (top_managers.py)
4. جلب حالة الإصابات (injuries_source.py)
5. تشغيل محرك القرار (decision_engine.py)
6. بناء التقرير (report_builder.py)
7. إرسال عبر تيليجرام (telegram_notifier.py)
8. حفظ نسخة من التقرير في data/history/gw_{n}.json (أرشفة للمراجعة اللاحقة)
```

---

## `.github/workflows/weekly_run.yml`

**المتطلبات:**
- يعمل تلقائياً حسب جدول زمني ثابت (مثال: كل يوم جمعة الساعة 18:00 UTC — يُعدَّل بحيث يكون قبل الديدلاين بساعات كافية).
- يقرأ الأسرار من GitHub Secrets: `FPL_TEAM_ID`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`.
- خطوات: `checkout` → `setup-python` → `pip install -r requirements.txt` → `python src/main.py`.
- يُضاف أيضاً خيار `workflow_dispatch` للتشغيل اليدوي وقت الاختبار.

---

## خطة البناء المرحلية (الترتيب المقترح للـ AI agent)

| المرحلة | الوحدات | هدف الاختبار |
|---|---|---|
| 1 | `config.py` + `fpl_client.py` | التأكد من جلب تشكيلتي وبيانات اللاعبين بنجاح |
| 2 | `squad_optimizer.py` | (مرة واحدة فقط) بناء تشكيلة الجولة الأولى واختبارها ضمن قيود الميزانية |
| 3 | `injuries_source.py` | التأكد من جلب حالات الإصابة بشكل صحيح |
| 4 | `top_managers.py` | التأكد من جلب أفضل 10 ومقارنتهم (يُختبر بعد أول جولة فعلياً) |
| 5 | `transfer_planner.py` | اختبار منطق الطابور والحفظ/الاسترجاع من `data/state/` |
| 6 | `decision_engine.py` | اختبار القواعد بـ unit tests ببيانات وهمية (`tests/`) |
| 7 | `report_builder.py` + `telegram_notifier.py` | التأكد من وصول رسالة تجريبية فعلية لتيليجرام |
| 8 | `main.py` + دمج كل شيء | تشغيل كامل يدوي محلياً |
| 9 | `.github/workflows/weekly_run.yml` | تشغيل تجريبي عبر `workflow_dispatch` قبل الاعتماد على الجدولة التلقائية |

---

## قيود ومبادئ يجب أن يلتزم بها الـ AI agent أثناء البناء

1. **لا تنفيذ تلقائي داخل حساب FPL** — المشروع للقراءة والتحليل والإشعار فقط.
2. **لا مفاتيح API مدفوعة أو اشتراكات** — كل الأدوات المستخدمة مجانية بالكامل.
3. **لا AI API خارجي** في هذه النسخة — كل المنطق rules-based بايثون عادي.
4. **كل الأسرار (Token, Team ID) تُقرأ من متغيرات بيئة/GitHub Secrets فقط** — ممنوع كتابتها مباشرة بالكود.
5. **التقرير يُعرض دائماً كـ"اقتراح للمراجعة"** وليس أمراً نهائياً، خصوصاً بند `flagged_players`.
6. **معالجة الأخطاء:** أي فشل باتصال (FPL API أو مصدر الإصابات أو تيليجرام) يجب أن يُسجَّل بوضوح ولا يوقف التشغيل بصمت.

---

## ملاحظة ختامية للمستخدم

هذا الملف جاهز لتسليمه مباشرة لأي AI coding agent (Claude Code، Cursor، إلخ) كنقطة انطلاق. يُفضّل تسليمه المرحلة الأولى فقط أولاً (`config.py` + `fpl_client.py`) والتأكد من نجاحها قبل الانتقال للمرحلة التالية، بدل طلب بناء كل شيء دفعة واحدة.
